<section id="vision" class="vision">
  <div class="vision-background bg-pattern-gold-dual"></div>
  <div class="container">
    <div class="vision-content">
      <h2 class="section-title">
        <span class="title-decoration title-decoration--light">ビジョン</span>
      </h2>
      <div class="vision-message">
        <p class="vision-message-text">
          まちにある色々なカタチの知識を未来につなぐ
        </p>
      </div>
      <div class="vision-text">
        <p>
          データをベクトルに変換することで、可能性を無限に広げられる。
          私たちは、ベクトルデータを核としたサービスを提供することで、「まち」にある色々なカタチの「知識」を未来につなぐことができると考えています。
        </p>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  @use '../styles/scss/_breakpoints.scss' as *;
  .vision {
    position: relative;
    color: var(--color-white);
    overflow: hidden;
  }

  .vision-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--color-indigo);
    z-index: -1;
  }

  .vision-content {
    max-width: 100%;
    margin: 0 auto;
  }

  .section-title {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.8rem;
    color: var(--color-white);
  }

  /* Title decoration styles moved to global.scss */

  .vision-message-text {
    font-size: 1.2rem;
    font-weight: 700;
    line-height: 1.5;
    margin: 0 0 1.5rem;
    padding: 0 1rem;
    color: var(--color-gold);
    text-align: center;
  }

  .vision-text {
    font-size: 1rem;
    line-height: 1.7;
  }

  .vision-text p {
    margin-bottom: 1.2rem;
  }

  @media (min-width: $breakpoint-481) {
    /* Removed custom padding - now using global section padding */

    .section-title {
      margin-bottom: 2.5rem;
      font-size: 2rem;
    }

    .vision-message-text {
      font-size: 1.4rem;
      margin: 0 0 2rem;
      padding: 0 1.5rem;
    }

    .vision-text {
      font-size: 1.05rem;
    }
  }

  @media (min-width: $breakpoint-769) {
    /* Removed custom padding - now using global section padding */

    /* Responsive background pattern now handled by utility classes */

    .vision-content {
      max-width: 900px;
    }

    .section-title {
      margin-bottom: 3rem;
      font-size: 2.2rem;
    }

    /* Responsive title decoration styles moved to global.scss */

    .vision-message-text {
      font-size: 1.8rem;
      margin: 0 0 2.5rem;
      padding: 0 2rem;
    }

    .vision-text {
      font-size: 1.1rem;
      line-height: 1.8;
    }

    .vision-text p {
      margin-bottom: 1.5rem;
    }
  }
</style>
