<section id="hero" class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>All with RAG!</h1>
      <p class="hero-subtitle">ベクトル類似性検索技術により、<br>紙、PDF、画像、音声、すべての情報を<br>AIが使いやすい形に変換します。</p>
      <div class="hero-buttons">
        <a href="#contact" class="button">お問い合わせ</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: #fff;
    color: #14213d;
    min-height: 50vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
  }

  .hero-content {
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .hero h1 {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    margin-bottom: 2.5rem;
    color: #14213d;
  }

  .hero-buttons {
    display: flex;
    justify-content: center;
  }

  .button {
    background: #14213d;
    color: #fff;
    font-size: 1.25rem;
    font-weight: 600;
    padding: 1rem 2.5rem;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    text-decoration: none;
    transition: background 0.2s;
  }

  .button:hover {
    background: #22325a;
  }

  .integrations {
    margin-top: 2.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 0.7;
  }
  .integrations-title {
    font-size: 1.1rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: #888;
    margin-bottom: 0.5rem;
  }
  .qdrant-logo-wrap {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.2rem;
  }
  .qdrant-logo {
    height: 28px;
    width: auto;
    filter: grayscale(1) brightness(0.7);
    opacity: 0.8;
    margin-bottom: 0.1rem;
  }
  .qdrant-caption {
    font-size: 0.85rem;
    color: #888;
    margin-top: 0.1rem;
  }
</style>
