<section id="hero" class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>All with RAG!</h1>
      <p class="hero-subtitle">ベクトル類似性検索技術により、<br>紙、PDF、画像、音声、すべての情報を<br>AIが使いやすい形に変換します。</p>
      <div class="hero-buttons">
        <a href="#contact" class="button button-primary">お問い合わせ</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: var(--color-white);
    color: var(--color-indigo-dark);
    min-height: 50vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
  }

  .hero-content {
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
  }

  .hero h1 {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    margin-bottom: 2.5rem;
    color: var(--color-indigo-dark);
  }

  .hero-buttons {
    display: flex;
    justify-content: center;
  }

  /* Remove the custom .button styles - now using global button system */

</style>
